spring:
  application:
    name: inventoryservice

server:
  servlet:
    context-path: /inventory
  port: 8083

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health, info
    
